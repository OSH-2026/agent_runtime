[package]
name = "actions"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
async-trait = "0.1"
prost = "0.13"
tokio = { version = "1.38", features = ["rt", "macros"] }
tonic = "0.12"

[build-dependencies]
tonic-build = "0.12"
