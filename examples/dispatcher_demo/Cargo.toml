[package]
name = "dispatcher_demo"
version = "0.1.0"
edition = "2024"

[dependencies]
async-trait = "0.1"
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
actions = { path = "../../rust/actions" }
dispatcher = { path = "../../rust/dispatcher" }
